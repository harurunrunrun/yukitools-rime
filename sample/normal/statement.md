# Two Integers

整数 A, B が与えられます。A + B を出力してください。

## 制約

- -10^9 ≤ A, B ≤ 10^9

## 入力

```text
A B
```

## 出力

和を1行で出力してください。

## 例

入力 `1 2` に対して出力は `3` です。

## 部分点

`small_*` のケースでは |A|, |B| ≤ 10、30点です。
`large_*` のケースでは追加制約はなく、70点です。
部分点APIの設定例は同じディレクトリの `subtask.json` にあります。
