# Two Positive Integers

整数 N に対して、X + Y = N を満たす正の整数 X, Y を1組出力してください。
複数ある場合は、どれを出力してもかまいません。

## 制約

- 2 ≤ N ≤ 10^9
- 1 ≤ X, Y < N

## 入力

N が1行に与えられます。

## 出力

`X Y` を1行で出力してください。

## 例

入力 `17` に対して `1 16` も `16 1` も正解です。`0 17` は不正解です。
